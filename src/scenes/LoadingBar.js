import Phaser from "phaser";

class LoadingBar extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'LoadingBar'
        });
    }

    preload() {
    }

    create() {
    }

    update(delta, time) {
    }
}

export default LoadingBar;